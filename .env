# Temporary environment file for local development
# The app uses in-memory storage, so this is just a placeholder
DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy
NODE_ENV=development
PORT=5000
